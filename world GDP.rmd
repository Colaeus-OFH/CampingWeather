---
title: "ECON 101 World GDP notebook"
output: html_notebook
---
Doug Merrett ⓒ 2022 OFH Consulting

```{r}
library("dplyr")
library("lubridate")
library("timeDate")
library("vetiver")
library("DBI")
library("RSQLite")

setRmetricsOptions(myFinCenter = "Halifax")
today <- as.Date(Sys.timeDate())
```
# Load dataset
```{r}
WorldGDP <- read.csv("/home/dougm/Desktop/wid_pretax_usd21_wide.csv")
names(WorldGDP)
CAN_GDP <- filter(WorldGDP, full_name == "Canada")
plot(CAN_GDP$year, CAN_GDP$inc_decile_p70p80)
CAN_GDP$RichPoor <- CAN_GDP$inc_decile_p90p100/CAN_GDP$inc_decile_p0p10
plot(CAN_GDP$year,CAN_GDP$RichPoor)
```